---
export interface Props {
   title: string;
}

const { title } = Astro.props;

import "@fontsource/koho";
import "@fontsource/koho/500.css";
import "@fontsource/koho/700.css";
---

<!DOCTYPE html>
<html lang="en">
   <head>
      <meta charset="UTF-8" />
      <meta name="viewport" content="width=device-width" />
      <link
         rel="icon"
         type="image/png"
         href="/detalle-proyecto/ayllu-kaypi.png"
      />
      <meta name="generator" content={Astro.generator} />
      <title>{title}</title>
      <!-- <link
         rel="stylesheet"
         href="https://unpkg.com/leaflet@1.9.3/dist/leaflet.css"
         integrity="sha256-kLaT2GOSpHechhsozzB+flnD+zUyjE2LlfWPgU04xyI="
         crossorigin=""
      />
      <script
         src="https://unpkg.com/leaflet@1.9.3/dist/leaflet.js"
         integrity="sha256-WBkoXOwTeyKclOHuWtc+i2uENFpDZ9YPdf5Hf+D7ewM="
         crossorigin=""
      ></script> -->
      <!-- <script src='https://unpkg.com/panzoom@9.4.0/dist/panzoom.min.js'></script> -->
   </head>
   <body class="bg-[#010B15] bg-homepage-hero-after overflow-x-hidden">
      <slot />
   </body>
</html>

<style>
   body {
      --sb-track-color: #232e33;
      --sb-thumb-color: #2ee5d8;
      --sb-size: 10px;

      scrollbar-color: var(--sb-thumb-color) var(--sb-track-color);
   }

   body::-webkit-scrollbar {
      width: var(--sb-size);
   }

   body::-webkit-scrollbar-track {
      background: var(--sb-track-color);
      border-radius: 10px;
   }

   body::-webkit-scrollbar-thumb {
      background: var(--sb-thumb-color);
      border-radius: 10px;
   }
</style>
